export const forgotPasswordState = {};

const forgotPasswordReducer = (state = forgotPasswordState, action) => {
  switch (action.type) {
  }
  return state;
};

export default forgotPasswordReducer;
